// app/match-detail/[matchId].tsx (or similar)
import MatchDetailScreen from '../../screens/MatchDetailScreen';

export default function MatchDetailRoute() {
  return <MatchDetailScreen />;  // ✅ no props here
}